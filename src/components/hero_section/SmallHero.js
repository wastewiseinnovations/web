import React from 'react'
import { HeroContainer, HeroBg, VideoBg, HeroContent, Img, HeroP, ImgWrapper} from './SmallHeroElems'

const SmallHero = () => {
  return (
    <HeroContainer>
      <HeroContent>
        <h1>Simulador</h1>
      </HeroContent>
    </HeroContainer>
  )
}

export default SmallHero